APP_ENV=production
APP_BASE_URL=http://tapp.lcstudio-incubate.co.za
APP_TIMEZONE=Africa/Johannesburg
APP_API_BASE_PATH=/api

DB_HOST=sql63.jnb2.host-h.net
DB_PORT=3306
DB_NAME=digitcexse_db2
DB_USER=digitcexse_2
DB_PASS=UfMEcUsfi1Mk8d9UEgg8

HMAC_SECRET=

CORS_ALLOWED_ORIGINS=

LOG_PATH=storage/logs/app.log

# Welcome-email delivery (no package required). Replace the example values
# with the SMTP details supplied by your mailbox provider.
# Gmail example: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587 and an App Password
# created at https://myaccount.google.com/apppasswords (not your Gmail password).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM_EMAIL=
MAIL_FROM_NAME=TAPP Administration

# Browser-only email through the EmailJS CDN. These values are safe to expose
# to the browser, but should only be used after creating an EmailJS account.
EMAILJS_PUBLIC_KEY=ELjwBd3To7ln4Pv_u
EMAILJS_SERVICE_ID=service_w1aum3b
EMAILJS_TEMPLATE_ID=template_p813bmb
